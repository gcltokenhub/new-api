# Token 工厂协作规则

## 当前阶段

本仓库当前有效的一期需求为 `docs/requirements/v1-platform-foundation-prd.md`。原 `v1-prd.md` 已由 CR-001 覆盖，保留为后续企业产品层蓝图。确认需求不等于已完成源码基线、实现或部署。

## 工作边界

- 后端优先复用 New-API；企业、项目、模型开通、额度归属和成本口径需按需求定义，不能假定 New-API 用户或分组自动等同于企业租户。
- 对照选定源码版本核实接口、权限与数据模型，禁止编造已存在的 API。
- 所有项目数据访问都须验证企业及项目归属；管理员上游凭证不得进入普通用户响应。
- 前端新增与修改默认遵循 [前端视觉与交互规范](docs/design/frontend-visual-guidelines.md)，延续 Figma 门户的蓝绿配色、清晰字阶与轻量卡片；控制台按业务密度适配。
- 设计来源见 docs/requirements/sources.md。设计示例中的模型价格、节点指标和 SLA 不得直接当作真实产品承诺。
- Figma 页面必须逐段实现为语义化组件，正文可选择、入口可点击并支持键盘操作。禁止整页截图、区块截图或透明热点覆盖替代真实页面；图片仅作为独立内容或装饰素材。
- 前端日常修改使用开发服务与热更新，代理现有 Docker 后端；不要为每次页面修改重建镜像或重启服务。启动方式见 docs/architecture/development-compose.md。
- 同一变更同步更新受影响的需求、页面说明、接口文档和验收追踪。
- 不提交账号密码、API Key、Cookie、生产日志或真实联系人信息。
- Git 变更使用明确文件列表；不要混入用户未授权的工作。保留上游历史，不强推或改写已共享历史。

## 验证

文档检查包括相对链接、需求编号、验收映射、来源、范围一致性和 `git diff --check`。
前端命令见 web/package.json，改动执行受影响测试、类型检查与文件 lint，并遵循 web/AGENTS.md。首页验证记录见 docs/testing/v1-platform-foundation-acceptance.md；不得将文档或前端检查描述为真实模型业务测试通过。

## Windows 本地环境

PowerShell 进入项目目录失败时应立即停止后续相对路径操作；使用明确的绝对路径，并检查命令退出码。不要因目录切换失败而在磁盘根目录执行搜索、初始化或文件修改。
